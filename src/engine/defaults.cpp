#include "defaults.hpp"

namespace Engine::Defaults {
    UI::Font *font = nullptr;
    Graphics::Shader* shader = nullptr;
    Graphics::Shader* textShader = nullptr;
    Graphics::Texture* texture = nullptr;
}